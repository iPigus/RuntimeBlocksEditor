using UnityEngine;

namespace RuntimeBlocksEditor.Gizmo
{
    /// <summary>
    /// Represents a group of blocks that should be treated as a single entity.
    /// </summary>
    public class BlockGroup : MonoBehaviour
    {
        // Add any group-specific functionality here if needed
    }
} 